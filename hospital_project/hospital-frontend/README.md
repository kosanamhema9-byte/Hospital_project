# Hospital Frontend (Vite + React)

## Setup
1. Install:
   ```
   npm install
   ```
2. Start dev server:
   ```
   npm run dev
   ```
By default the frontend will call `http://localhost:5000`. You can change it in `.env`.
